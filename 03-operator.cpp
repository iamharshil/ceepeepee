#include <iostream>
using namespace std;

int main()
{
    int x = 100 + 50;

    int sum1 = 100 + 50;
    int sum2 = sum1 + 250;
    int sum3 = sum2 + sum2;

    // Aerithmetic + - * / % ++ --
    // assignment = += -= *= /= %= &= |= ^= >>= <<=
    // comparison == != > < >= <=
    // logic && || !
    return 0;
}